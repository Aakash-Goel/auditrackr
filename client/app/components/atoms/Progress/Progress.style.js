const progressStyles = () => {
  const styleObject = {
    // bar wrapper styles
    root: {},

    // bar styles
    bar: {},

    // color primary styles
    primary: {},
    primaryBackground: {},
    primaryBufferBackground: {},

    // color secondary styles
    secondary: {},
    secondaryBackground: {},
    secondaryBufferBackground: {},
  };
  return styleObject;
};

export default progressStyles;
