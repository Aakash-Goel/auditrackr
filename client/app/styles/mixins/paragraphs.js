import { defaultFont } from '../variables';

const paragraphsStyles = {
  default: {
    font: defaultFont,
    margin: 0,
    marginBottom: '8px',
    marginTop: '8px',
  },
};

export default paragraphsStyles;
